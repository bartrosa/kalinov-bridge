/--
This module is temporarily edited by the Python demo runner (`kalinov-bridge run-demo`).
The committed version keeps `sorry`; the mock replaces it with `trivial`, then restores the file.
-/

theorem runner_target : True := by sorry
